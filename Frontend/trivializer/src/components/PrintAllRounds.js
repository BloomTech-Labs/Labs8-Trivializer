import React, { Component } from "react;";
import { connect } from "react-redux";

class PrintAllRounds extends Component {
  constructor(props) {
    super(props);
  }
}

export default connect(
  mapStateToProps,
  {}
)(PrintAllRounds);
